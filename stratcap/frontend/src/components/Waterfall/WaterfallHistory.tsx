import React from 'react';
import { Box, Typography } from '@mui/material';

const WaterfallHistory: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">WaterfallHistory</Typography>
    </Box>
  );
};

export default WaterfallHistory;
