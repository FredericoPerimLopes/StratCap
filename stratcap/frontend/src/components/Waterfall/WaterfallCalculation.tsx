import React from 'react';
import { Box, Typography } from '@mui/material';

const WaterfallCalculation: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">WaterfallCalculation</Typography>
    </Box>
  );
};

export default WaterfallCalculation;
