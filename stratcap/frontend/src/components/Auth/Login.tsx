import React from 'react';
import { Box, Typography } from '@mui/material';

const Login: React.FC = () => {
  return (
    <Box>
      <Typography variant="h4">Login</Typography>
      <Typography>Login component placeholder</Typography>
    </Box>
  );
};

export default Login;